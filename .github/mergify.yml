pull_request_rules:
  - name: automatic merge if author is foo
    conditions:
      - author=restyled-commits
      - status-success=Travis CI - Pull Request
    actions:
      merge:
        method: rebase
